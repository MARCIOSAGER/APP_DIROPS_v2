// Stub: Send daily consolidated reports - requires email integration
export default async function enviarRelatoriosConsolidadosDiarios(params) {
  console.warn('[enviarRelatoriosConsolidadosDiarios] Funcao ainda nao implementada como Edge Function. Params:', params);
  return { success: false, message: 'Envio de relatorios consolidados diarios nao disponivel. Configure a Edge Function no Supabase.' };
}
