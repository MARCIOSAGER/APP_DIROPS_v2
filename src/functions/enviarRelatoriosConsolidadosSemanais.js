// Stub: Send weekly consolidated reports - requires email integration
export default async function enviarRelatoriosConsolidadosSemanais(params) {
  console.warn('[enviarRelatoriosConsolidadosSemanais] Funcao ainda nao implementada como Edge Function. Params:', params);
  return { success: false, message: 'Envio de relatorios consolidados semanais nao disponivel. Configure a Edge Function no Supabase.' };
}
