// Stub: Send monthly consolidated reports - requires email integration
export default async function enviarRelatoriosConsolidadosMensais(params) {
  console.warn('[enviarRelatoriosConsolidadosMensais] Funcao ainda nao implementada como Edge Function. Params:', params);
  return { success: false, message: 'Envio de relatorios consolidados mensais nao disponivel. Configure a Edge Function no Supabase.' };
}
