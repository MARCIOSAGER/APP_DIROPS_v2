---
phase: 02-flightaware-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/operacoes/VoosTable.jsx
  - src/components/operacoes/CacheVooFlightAwareList.jsx
autonomous: true
requirements:
  - FA-01
  - FA-02
  - FA-03
  - FA-04

must_haves:
  truths:
    - "Flights imported from FlightAware with empty registration show amber 'Verificar Registo' badge"
    - "Flights with empty horario_previsto show amber 'Verificar Horarios' badge"
    - "FlightAware-sourced flights show blue 'Dados FlightAware' badge distinct from warning badges"
    - "All 3 badges can stack simultaneously on a single flight row"
    - "User can toggle 'Apenas voos reais' on cache list to hide flights without actual_off/actual_on and cancelled flights"
    - "Toggle is OFF by default showing all cached flights"
    - "Cache status dropdown includes 'atualizado' option"
  artifacts:
    - path: "src/components/operacoes/VoosTable.jsx"
      provides: "FA origin badge, verification badges"
      contains: "Dados FlightAware"
    - path: "src/components/operacoes/CacheVooFlightAwareList.jsx"
      provides: "Real flights toggle filter, atualizado status"
      contains: "filtroVoosReais"
  key_links:
    - from: "src/components/operacoes/VoosTable.jsx"
      to: "voo.created_by === 'FlightAware' || voo.origem_dados === 'FlightAware'"
      via: "isFA detection inline"
      pattern: "isFA.*Dados FlightAware"
    - from: "src/components/operacoes/CacheVooFlightAwareList.jsx"
      to: "raw_data.actual_off || raw_data.actual_on"
      via: "filtroVoosReais state toggle"
      pattern: "filtroVoosReais"
---

<objective>
Add FlightAware visual indicators (badges) to the flight list and a "real flights only" toggle filter to the cache list.

Purpose: Users need to quickly identify FlightAware-sourced flights, spot missing data (registration, schedule times), and filter the cache to show only flights that actually operated.
Output: Enhanced VoosTable with 3 stackable badges, enhanced CacheVooFlightAwareList with real-flights toggle and atualizado status support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-flightaware-ui/02-CONTEXT.md

@src/components/operacoes/VoosTable.jsx
@src/components/operacoes/CacheVooFlightAwareList.jsx

<interfaces>
<!-- VoosTable badge detection pattern (line 208): -->
const isFA = voo.created_by === 'FlightAware' || voo.origem_dados === 'FlightAware';

<!-- Existing badges in VoosTable: -->
<!-- Line 229: Small "FA" badge (sky-400/sky-600) next to flight number -->
<!-- Line 243-245: "Verificar Registo" badge (amber, outline, text-[9px]) in registration cell -->
<!-- Line 254-256: "Verificar Horario" badge (amber, outline, text-[9px]) in time cell -->

<!-- CacheVooFlightAwareList filter state pattern: -->
const [filtroStatus, setFiltroStatus] = useState('todos');
const [filtroBusca, setFiltroBusca] = useState('');

<!-- CacheVooFlightAwareList statusColors object (line 15-19): -->
const statusColors = {
  pendente: { bg: 'bg-yellow-50', text: 'text-yellow-800', badge: 'bg-yellow-100 text-yellow-800' },
  importado: { bg: 'bg-green-50', text: 'text-green-800', badge: 'bg-green-100 text-green-800' },
  rejeitado: { bg: 'bg-red-50', text: 'text-red-800', badge: 'bg-red-100 text-red-800' }
};

<!-- CacheVooFlightAwareList voosFiltratos filter (line 237-252): -->
<!-- Currently hides cancelled and flights without any datetime fields -->
<!-- Filter chain: statusMatch && buscaMatch && aeroportoMatch && dataInicioMatch && dataFimMatch -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Dados FlightAware" badge and verify existing badges in VoosTable</name>
  <files>src/components/operacoes/VoosTable.jsx</files>
  <read_first>
    - src/components/operacoes/VoosTable.jsx (current badge implementation lines 208-256)
    - src/components/ui/badge.jsx (Badge component API)
  </read_first>
  <action>
    In VoosTable.jsx, modify the flight row rendering (inside currentVoos.map, around lines 215-275):

    1. **Replace the small "FA" badge** (line 229) with a full "Dados FlightAware" badge per D-01:
       - Remove: `{isFA && <Badge variant="outline" className="text-[9px] border-sky-400 text-sky-600 px-1 py-0">FA</Badge>}`
       - Add in its place: `{isFA && <Badge variant="outline" className="text-[9px] border-blue-400 text-blue-700 bg-blue-50 px-1 py-0">Dados FlightAware</Badge>}`
       - This uses blue info style (border-blue-400 text-blue-700 bg-blue-50) per D-01

    2. **Verify existing "Verificar Registo" badge** (lines 243-245) is correct:
       - Condition: `isFA && !voo.registo_aeronave` — this satisfies FA-01
       - Style: amber outline text-[9px] — matches D-03, no change needed

    3. **Verify existing "Verificar Horario" badge** (lines 254-256) is correct:
       - Condition: `isFA && !voo.horario_previsto` — this satisfies FA-02
       - Style: amber outline text-[9px] — matches D-03, no change needed

    4. **Ensure all 3 badges can stack** per D-02:
       - The "Dados FlightAware" badge is in the numero_voo cell (div with flex items-center gap-1)
       - "Verificar Registo" is in the registo_aeronave cell
       - "Verificar Horario" is in the horario_previsto cell
       - Since they are in different cells, they naturally stack — no layout conflict

    Per D-01, D-02, D-03: Blue info badge for FA origin, amber warning badges for missing data, all can appear simultaneously.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/operacoes/VoosTable.jsx','utf8'); const checks=['Dados FlightAware','border-blue-400 text-blue-700 bg-blue-50','Verificar Registo','Verificar Hor']; const results=checks.map(s=>c.includes(s)); console.log(JSON.stringify(Object.fromEntries(checks.map((k,i)=>[k,results[i]])))); if(!results.every(Boolean)) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - VoosTable.jsx contains the string `Dados FlightAware` (not just `FA`)
    - VoosTable.jsx contains `border-blue-400 text-blue-700 bg-blue-50` on the Dados FlightAware badge
    - VoosTable.jsx still contains `Verificar Registo` badge with `border-amber-400 text-amber-700 bg-amber-50`
    - VoosTable.jsx still contains `Verificar Hor` badge with `border-amber-400 text-amber-700 bg-amber-50`
    - The old `>FA</Badge>` text-only badge no longer exists in VoosTable.jsx
  </acceptance_criteria>
  <done>FlightAware-sourced flights show blue "Dados FlightAware" badge, missing registration shows amber "Verificar Registo", missing schedule shows amber "Verificar Horario", all three can appear on the same row simultaneously</done>
</task>

<task type="auto">
  <name>Task 2: Add "Apenas voos reais" toggle and atualizado status to CacheVooFlightAwareList</name>
  <files>src/components/operacoes/CacheVooFlightAwareList.jsx</files>
  <read_first>
    - src/components/operacoes/CacheVooFlightAwareList.jsx (full file — filter logic lines 237-252, status dropdown lines 430-441, statusColors lines 15-19, clear filters lines 444-453)
  </read_first>
  <action>
    In CacheVooFlightAwareList.jsx, make these changes:

    1. **Add `atualizado` to statusColors** (after line 18, before the closing `}`):
       ```
       atualizado: { bg: 'bg-blue-50', text: 'text-blue-800', badge: 'bg-blue-100 text-blue-800' }
       ```

    2. **Add new state for the real flights toggle** (after `const [filtroAeroporto, setFiltroAeroporto] = useState('');` around line 30):
       ```
       const [filtroVoosReais, setFiltroVoosReais] = useState(false);
       ```
       Default is false per D-05 (OFF by default — all cached flights shown initially).

    3. **Add real-flights filter to voosFiltratos** (inside the filter function, around line 251, before the final return):
       Add this condition after `const dataFimMatch`:
       ```
       const voosReaisMatch = !filtroVoosReais || (
         (rawData.actual_off || rawData.actual_on) && !rawData.cancelled
       );
       ```
       And update the return to: `return statusMatch && buscaMatch && aeroportoMatch && dataInicioMatch && dataFimMatch && voosReaisMatch;`

       Per D-06: "Real flights" = actual_off OR actual_on present in raw_data, AND cancelled flights hidden.

    4. **Add toggle switch UI** in the filter bar. Inside the `<CardContent className="p-3 sm:p-4">` filter area (after the status dropdown `</div>` around line 442, before the clear button), add:
       ```jsx
       <div className="flex items-center gap-2 pt-5">
         <label className="relative inline-flex items-center cursor-pointer">
           <input
             type="checkbox"
             checked={filtroVoosReais}
             onChange={(e) => setFiltroVoosReais(e.target.checked)}
             className="sr-only peer"
           />
           <div className="w-9 h-5 bg-gray-200 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full rtl:peer-checked:after:-translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:start-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:bg-blue-600"></div>
           <span className="ms-2 text-xs sm:text-sm font-medium text-slate-700">Apenas voos reais</span>
         </label>
       </div>
       ```
       Per D-04: binary on/off toggle, always visible in filter bar.

    5. **Add `atualizado` to status filter dropdown** (after the `rejeitado` option, around line 440):
       ```
       <option value="atualizado">{t('cacheFA.atualizado') || 'Atualizado'}</option>
       ```

    6. **Reset filtroVoosReais on clear filters** (add `setFiltroVoosReais(false);` inside the clear button onClick, around line 452).

    7. **Import Switch component is NOT needed** — using a pure CSS/Tailwind toggle to avoid new dependency. The toggle uses the standard peer checkbox pattern common in Tailwind UIs.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/operacoes/CacheVooFlightAwareList.jsx','utf8'); const checks=['filtroVoosReais','actual_off','actual_on','Apenas voos reais','atualizado','voosReaisMatch','setFiltroVoosReais(false)']; const results=checks.map(s=>c.includes(s)); console.log(JSON.stringify(Object.fromEntries(checks.map((k,i)=>[k,results[i]])))); if(!results.every(Boolean)) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - CacheVooFlightAwareList.jsx contains `const [filtroVoosReais, setFiltroVoosReais] = useState(false)`
    - CacheVooFlightAwareList.jsx contains `actual_off` and `actual_on` in the voosReaisMatch condition
    - CacheVooFlightAwareList.jsx contains `Apenas voos reais` label text
    - CacheVooFlightAwareList.jsx contains `voosReaisMatch` in the filter return chain
    - CacheVooFlightAwareList.jsx statusColors object contains `atualizado` key with `bg-blue-50` value
    - CacheVooFlightAwareList.jsx status dropdown contains `atualizado` option
    - CacheVooFlightAwareList.jsx clear filters handler contains `setFiltroVoosReais(false)`
  </acceptance_criteria>
  <done>Cache list has "Apenas voos reais" toggle (OFF by default) that filters to flights with actual_off/actual_on and hides cancelled; status dropdown and statusColors include "atualizado" for merged flights</done>
</task>

</tasks>

<verification>
1. VoosTable shows blue "Dados FlightAware" badge for FA flights, amber "Verificar Registo" for missing registration, amber "Verificar Horario" for missing schedule
2. All three badges can appear simultaneously on the same row
3. CacheVooFlightAwareList has toggle that defaults to OFF
4. When toggled ON, only flights with actual_off or actual_on are shown, cancelled hidden
5. Status dropdown includes "atualizado" option
6. App builds without errors: `npx vite build`
</verification>

<success_criteria>
- Blue "Dados FlightAware" badge visible on FA-sourced flights (replaces small "FA" label)
- Amber verification badges unchanged and still functional
- "Apenas voos reais" toggle present in cache filter bar, OFF by default
- Toggling ON hides flights without actual_off/actual_on and cancelled flights
- "atualizado" status recognized in cache list with blue styling
</success_criteria>

<output>
After completion, create `.planning/phases/02-flightaware-ui/02-01-SUMMARY.md`
</output>
