---
phase: 04-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/KPIsOperacionais.jsx
  - src/pages/LogAuditoria.jsx
  - src/pages/LogAuditoriaDetalhes.jsx
  - src/pages/GestaoAcessos.jsx
  - src/pages/GestaoNotificacoes.jsx
  - src/pages/GestaoAPIKeys.jsx
  - src/pages/GestaoEmpresas.jsx
  - src/pages/ConfiguracaoTarifas.jsx
  - src/pages/ConfiguracoesGerais.jsx
  - src/pages/Credenciamento.jsx
  - src/pages/Lixeira.jsx
  - src/pages/Suporte.jsx
  - src/pages/GuiaUtilizador.jsx
  - src/pages/FlightAware.jsx
  - src/pages/ServicosAeroportuarios.jsx
  - src/pages/HistoricoAcessoDocumentos.jsx
  - src/pages/PowerBi.jsx
  - src/pages/Monitoramento.jsx
  - src/pages/ImportacaoAiaan.jsx
  - src/pages/Home.jsx
  - src/pages/GerirPermissoes.jsx
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "All remaining pages (KPIs, LogAuditoria, GestaoAcessos, GestaoNotificacoes, GestaoAPIKeys, GestaoEmpresas, ConfiguracaoTarifas, ConfiguracoesGerais, Credenciamento, Lixeira, Suporte, GuiaUtilizador, FlightAware, ServicosAeroportuarios, HistoricoAcessoDocumentos, PowerBi, Monitoramento, ImportacaoAiaan) have useI18n imported"
    - "Switching to EN renders English in page titles and primary action buttons for these pages"
  artifacts:
    - path: "src/pages/KPIsOperacionais.jsx"
      contains: "useI18n"
    - path: "src/pages/GestaoAcessos.jsx"
      contains: "useI18n"
    - path: "src/pages/GestaoEmpresas.jsx"
      contains: "useI18n"
  key_links:
    - from: "src/pages/GestaoEmpresas.jsx"
      to: "src/components/lib/i18n.jsx"
      via: "import { useI18n } from '@/components/lib/i18n'"
      pattern: "useI18n"
---

<objective>
Add i18n support to the remaining page files that currently display hardcoded Portuguese strings.

Purpose: DEBT-02 partial — admin/config pages and secondary operational pages need bilingual support to complete the requirement that switching to EN shows English across the entire app.

Output: 21 page files updated to use `const { t } = useI18n()` with page titles, buttons, and major labels wrapped in `t('key')`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Same i18n system as 04-02-PLAN — key mapping guide -->

Import pattern:
```javascript
import { useI18n } from '@/components/lib/i18n';
// Inside component:
const { t } = useI18n();
```

Relevant i18n.jsx key namespaces for these pages:
- `page.kpis.*` — KPIsOperacionais
- `page.gestao_acessos.*` — GestaoAcessos
- `page.gerir_permissoes.*` — GerirPermissoes
- `page.gestao_empresas.*` — GestaoEmpresas
- `page.config_tarifas.*` — ConfiguracaoTarifas
- `page.lixeira.*` — Lixeira
- `page.suporte.*` — Suporte
- `page.documentos.*` — HistoricoAcessoDocumentos (related)
- `page.home.title` — Home
- `flightaware.*` — FlightAware page strings
- `btn.*` — All common buttons
- `label.*` — All common labels
- `tab.*` — All tab names

Pages already using useI18n (verify before editing, skip if found):
- Home.jsx — check if it has useI18n; if yes, only fill gaps
- GerirPermissoes.jsx — already imports useI18n per line 24

For pages where no specific namespace exists in i18n.jsx, use `t('key')` with the English translation as the key fallback (t('key') returns 'key' if not found).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n to admin/config pages</name>
  <read_first>
    - src/pages/KPIsOperacionais.jsx (lines 1-50 for imports)
    - src/pages/LogAuditoria.jsx (lines 1-50)
    - src/pages/LogAuditoriaDetalhes.jsx (lines 1-50)
    - src/pages/GestaoAcessos.jsx (lines 1-50)
    - src/pages/GestaoNotificacoes.jsx (lines 1-50)
    - src/pages/GestaoAPIKeys.jsx (lines 1-50)
    - src/pages/GestaoEmpresas.jsx (lines 1-50)
    - src/pages/ConfiguracaoTarifas.jsx (lines 1-50)
    - src/pages/ConfiguracoesGerais.jsx (lines 1-50)
    - src/pages/GerirPermissoes.jsx (lines 1-30 — already has useI18n, check coverage)
  </read_first>
  <files>
    src/pages/KPIsOperacionais.jsx,
    src/pages/LogAuditoria.jsx,
    src/pages/LogAuditoriaDetalhes.jsx,
    src/pages/GestaoAcessos.jsx,
    src/pages/GestaoNotificacoes.jsx,
    src/pages/GestaoAPIKeys.jsx,
    src/pages/GestaoEmpresas.jsx,
    src/pages/ConfiguracaoTarifas.jsx,
    src/pages/ConfiguracoesGerais.jsx,
    src/pages/GerirPermissoes.jsx
  </files>
  <action>
For each file:

1. Check if `useI18n` is already imported (GerirPermissoes.jsx already has it at line 24)
2. If not: add `import { useI18n } from '@/components/lib/i18n';`
3. If not: add `const { t } = useI18n();` inside the component

4. Replace hardcoded PT strings in JSX — priority targets:
   - Page titles (CardTitle, h1, h2 elements)
   - Page descriptions/subtitles
   - Tab labels
   - Button text (use `btn.*` keys)
   - Column headers in tables
   - Empty state messages
   - Filter labels

**KPIsOperacionais.jsx** — use `page.kpis.title`, `page.kpis.subtitle`, `btn.*` keys.
  Note: this file also has hardcoded role check (see 04-01-PLAN), which is handled separately there.

**GestaoAcessos.jsx** — use `page.gestao_acessos.title`, `page.gestao_acessos.subtitle`, `btn.*`.

**GestaoEmpresas.jsx** — use `page.gestao_empresas.title`, `page.gestao_empresas.subtitle`, `btn.new_empresa`.

**ConfiguracaoTarifas.jsx** — use `page.config_tarifas.title`, `page.config_tarifas.subtitle`.

**GerirPermissoes.jsx** — already imports useI18n. Scan for any remaining hardcoded PT strings in JSX and wrap them. Do not change existing t() calls.

Apply the "what NOT to translate" rules:
- Do not translate DB status values or enum strings
- Do not translate technical identifiers
- Do not translate JS string variables (only JSX text nodes and string props like `placeholder`)
  </action>
  <verify>
    <automated>grep -l "useI18n" /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/KPIsOperacionais.jsx /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/LogAuditoria.jsx /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/GestaoAcessos.jsx /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/GestaoEmpresas.jsx /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/ConfiguracaoTarifas.jsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The 5 files listed in the verify command all appear (wc -l returns 5)
    - All 10 files in this task have useI18n imported
    - `grep -c "t('" src/pages/GestaoEmpresas.jsx` returns 5+
    - `grep -c "t('" src/pages/ConfiguracaoTarifas.jsx` returns 5+
    - Page titles (CardTitle) in all 10 files use t() — no bare Portuguese h1/CardTitle text in JSX
  </acceptance_criteria>
  <done>10 admin/config page files have useI18n and use t() for major visible strings</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n to remaining pages (Lixeira, Suporte, FlightAware, ServicosAeroportuarios, HistoricoAcessoDocumentos, PowerBi, Monitoramento, ImportacaoAiaan, Credenciamento, GuiaUtilizador, Home)</name>
  <read_first>
    - src/pages/Lixeira.jsx (lines 1-50)
    - src/pages/Suporte.jsx (lines 1-50)
    - src/pages/FlightAware.jsx (lines 1-50)
    - src/pages/ServicosAeroportuarios.jsx (lines 1-50)
    - src/pages/HistoricoAcessoDocumentos.jsx (lines 1-50)
    - src/pages/PowerBi.jsx (lines 1-50)
    - src/pages/Monitoramento.jsx (lines 1-50)
    - src/pages/ImportacaoAiaan.jsx (lines 1-50)
    - src/pages/Home.jsx (lines 1-50 — check if already has useI18n)
    - src/pages/Credenciamento.jsx (lines 1-50)
    - src/pages/GuiaUtilizador.jsx (lines 1-50)
  </read_first>
  <files>
    src/pages/Lixeira.jsx,
    src/pages/Suporte.jsx,
    src/pages/FlightAware.jsx,
    src/pages/ServicosAeroportuarios.jsx,
    src/pages/HistoricoAcessoDocumentos.jsx,
    src/pages/PowerBi.jsx,
    src/pages/Monitoramento.jsx,
    src/pages/ImportacaoAiaan.jsx,
    src/pages/Home.jsx,
    src/pages/Credenciamento.jsx,
    src/pages/GuiaUtilizador.jsx
  </files>
  <action>
Apply the same pattern — add import, add hook, replace PT strings with t('key').

**Home.jsx** — check first if it already imports useI18n. If it does, only wrap any remaining hardcoded strings. Use `page.home.title` key.

**FlightAware.jsx** — use `flightaware.*` keys (extensive coverage exists in i18n.jsx).

**Lixeira.jsx** — use `page.lixeira.title`, `page.lixeira.subtitle`, `btn.*`.

**Suporte.jsx** — use `page.suporte.title`, `page.suporte.subtitle`.

**ServicosAeroportuarios.jsx** — use `page.servicos_aeroportuarios.title`, `btn.*`.

**HistoricoAcessoDocumentos.jsx** — use `btn.*` and `label.*` keys.

**PowerBi.jsx**, **Monitoramento.jsx**, **ImportacaoAiaan.jsx** — use `btn.*`, `label.*` keys for visible buttons and labels.

**Credenciamento.jsx** — use `page.credenciamento.title`, `btn.*`.

**GuiaUtilizador.jsx** — use `page.* ` keys and `btn.*`.

For any page where most content is static/informational (PowerBi, Monitoramento), the minimum acceptable output is:
- useI18n imported and called
- Page title/heading uses t()
- At least 3 visible strings wrapped with t()
  </action>
  <verify>
    <automated>for page in Lixeira Suporte FlightAware ServicosAeroportuarios HistoricoAcessoDocumentos PowerBi Monitoramento ImportacaoAiaan Credenciamento GuiaUtilizador Home; do grep -l "useI18n" "/d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/${page}.jsx" 2>/dev/null && echo "$page: OK" || echo "$page: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files show "OK" in the verify output
    - `grep "useI18n" src/pages/FlightAware.jsx` returns at least 1 match
    - `grep -c "t('" src/pages/FlightAware.jsx` returns 5+ (flightaware.* keys used)
    - Page titles in all files use t() — no bare Portuguese in h1/CardTitle JSX
    - No regressions: existing functionality unchanged (only string wrappers added)
  </acceptance_criteria>
  <done>All remaining page files have useI18n and use t() for visible strings</done>
</task>

</tasks>

<verification>
```bash
# Count pages WITHOUT useI18n — should be 0 for operational pages
for page in /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/*.jsx; do
  if ! grep -q "useI18n" "$page" 2>/dev/null; then
    basename "$page"
  fi
done
```
Expected: Only login/auth/public pages may lack useI18n (Login.jsx, ValidacaoAcesso.jsx, SolicitacaoPerfil.jsx, CredenciamentoPublico.jsx, FormularioReclamacaoPublico.jsx, PoliticaPrivacidade.jsx, TermosServico.jsx, AlterarSenha.jsx, BoasVindas.jsx, AguardandoAprovacao.jsx, PaginaInicial.jsx, portalservicos.jsx, PortalEmpresa.jsx). All operational pages must have it.
</verification>

<success_criteria>
- All 21 listed page files import and call useI18n
- Page titles, tab labels, and primary action buttons use t('key') in all files
- Switching to EN (localStorage.setItem('language','en')) causes titles and buttons in these pages to render in English
- No regressions in functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-tech-debt/04-03-SUMMARY.md` following the summary template.
</output>
