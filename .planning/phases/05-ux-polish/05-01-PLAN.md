---
phase: 05-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
  - src/components/dashboard/DashboardStats.jsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Dashboard KPI row shows all 7-8 cards at equal size with no overlapping text on a 1280px screen"
    - "KPI card labels are legible (not smaller than text-xs)"
    - "Only one source of truth renders KPI cards (no duplicated inline card markup in Home.jsx)"
    - "DashboardStats component and Home.jsx inline stats use the same card template"
  artifacts:
    - path: "src/components/dashboard/DashboardStats.jsx"
      provides: "Consolidated KPI grid with consistent typography"
      contains: "text-xs"
    - path: "src/pages/Home.jsx"
      provides: "Dashboard page that delegates KPI rendering to DashboardStats"
      contains: "<DashboardStats"
  key_links:
    - from: "src/pages/Home.jsx"
      to: "src/components/dashboard/DashboardStats.jsx"
      via: "Component import and usage"
      pattern: "DashboardStats"
---

<objective>
Consolidate and polish the Dashboard KPI layout so it uses a single consistent card template across Home.jsx and DashboardStats.jsx, with legible typography and stable grid sizing.

Purpose: Currently Home.jsx renders two separate KPI areas — an inline set of 7 cards AND a separate DashboardStats component that also renders 8 cards. This causes doubled state, inconsistent styling, and potential layout conflicts. The card labels use `text-[10px]` which is below the readable threshold.

Output: Single KPI grid source of truth (DashboardStats handles all 8 cards), consistent `text-xs` minimum label size, stable `xl:grid-cols-8` grid used throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Home.jsx
@src/components/dashboard/DashboardStats.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate KPI cards into DashboardStats — single source of truth</name>
  <files>src/components/dashboard/DashboardStats.jsx</files>
  <read_first>
    - src/components/dashboard/DashboardStats.jsx (full file — current card template)
    - src/pages/Home.jsx lines 530-650 (inline KPI cards to fold in)
  </read_first>
  <action>
    DashboardStats currently renders an 8-card grid driven by `statsData[]`. The problem: Home.jsx also renders a parallel inline block of 7 cards (lines ~532–640) using `dashboardStats` / `serverStats` props directly, bypassing DashboardStats entirely. This duplication needs to be resolved in DashboardStats.

    Add three props to DashboardStats signature: `dashboardStats`, `serverStats`, and `trends`.
    - `dashboardStats`: the `dashboardStats` state object from Home (fields: totalVoos, chegadasHoje, partidasHoje, taxaPontualidade, ocorrenciasAbertas, voosUnicosLigados, voosSemLink)
    - `serverStats`: raw server stats (fields: total_voos, chegadas_hoje, partidas_hoje, pontualidade, ligados, sem_link, total_passageiros)
    - `trends`: the `trends` computed object (fields: voos, pontualidade)

    Build a `TrendIndicator` sub-component inside DashboardStats (same logic as Home.jsx lines ~408-417):
    ```jsx
    const TrendIndicator = ({ trend }) => {
      if (!trend) return null;
      const isUp = trend.direction === 'up';
      return (
        <span className={`inline-flex items-center gap-0.5 text-[10px] font-medium ${isUp ? 'text-green-600' : 'text-red-600'}`}>
          {isUp ? <TrendingUp className="h-3 w-3" /> : <TrendingDown className="h-3 w-3" />}
          {trend.pct}%
        </span>
      );
    };
    ```

    Update `statsData[]` derivation: when `dashboardStats` or `serverStats` props are present, use them for more accurate values (same logic as Home.jsx lines ~540-605). Specifically:
    - Total voos: `serverStats?.total_voos ?? dashboardStats?.totalVoos ?? stats.movements`
    - Chegadas hoje: `serverStats?.chegadas_hoje ?? dashboardStats?.chegadasHoje ?? stats.chegadasHoje`
    - Partidas hoje: `serverStats?.partidas_hoje ?? dashboardStats?.partidasHoje ?? stats.partidasHoje`
    - Pontualidade: `(serverStats?.pontualidade ?? dashboardStats?.taxaPontualidade ?? stats.punctuality).toFixed(1) + '%'`
    - Ocorrências abertas: `dashboardStats?.ocorrenciasAbertas ?? stats.safetyIncidents`

    For cards that have a `trends` counterpart (voos, pontualidade), render `<TrendIndicator trend={trends?.voos} />` inline after the value using a `flex items-center gap-1.5` wrapper — exactly as done in Home.jsx.

    For the "Total Voos" card, add the sub-line `{N} ligados, {M} sem link` when `serverStats?.ligados > 0 || dashboardStats?.voosUnicosLigados > 0`, using `text-[9px] text-slate-400`.

    Fix label typography: change all `text-[10px]` occurrences to `text-xs` (12px) across the entire DashboardStats file.

    Keep the existing card layout unchanged: `grid grid-cols-2 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-8 gap-3`.
  </action>
  <verify>
    <automated>grep -n "text-\[10px\]" /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/components/dashboard/DashboardStats.jsx | wc -l</automated>
  </verify>
  <done>
    - DashboardStats.jsx accepts dashboardStats, serverStats, trends props
    - All `text-[10px]` replaced by `text-xs` in DashboardStats.jsx
    - TrendIndicator renders for voos and pontualidade cards
    - Sub-line "N ligados, M sem link" present in Total Voos card
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate KPI block from Home.jsx, wire props to DashboardStats</name>
  <files>src/pages/Home.jsx</files>
  <read_first>
    - src/pages/Home.jsx full file (to locate both the inline 7-card block at ~lines 531-640 and the DashboardStats import)
    - src/components/dashboard/DashboardStats.jsx (to confirm new props added in Task 1)
  </read_first>
  <action>
    Home.jsx currently has two separate rendering paths for KPIs:
    1. Lines ~515-530: skeleton loading state for KPI cards
    2. Lines ~530-640: an inline block rendering 7 individual Card elements (total voos, chegadas, partidas, pontualidade, ocorrências, inspecoes, passageiros) — THIS ENTIRE BLOCK must be removed
    3. Somewhere below: `<DashboardStats ...>` call

    Step 1: Remove the entire inline KPI block (lines ~530-640 in the `dashboardStats && <>` branch). This block starts with `<div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-7 gap-3">` and ends before the next major section.

    Step 2: Replace the removed block with a single `<DashboardStats>` call, passing the new props:
    ```jsx
    <DashboardStats
      voos={filteredVoos}
      ocorrencias={filteredOcorrencias}
      inspecoes={filteredInspecoes}
      calculosTarifa={filteredCalculosTarifa}
      isLoading={isLoadingStats}
      serverStats={serverStats}
      dashboardStats={dashboardStats}
      trends={trends}
    />
    ```

    Step 3: Find the existing `<DashboardStats ...>` call (the one previously below the inline block) and REMOVE it — there should only be one DashboardStats call total after this change.

    Step 4: Remove the local `TrendIndicator` component definition from Home.jsx (lines ~408-417) since it is now defined inside DashboardStats.

    Step 5: Clean up any now-unused imports that were only used in the removed inline cards (e.g., `PlaneLanding`, `PlaneTakeoff`, `ClipboardCheck` if no longer used elsewhere in the file — check carefully before removing).

    The loading skeleton section (lines ~515-529, grid of 8 Skeleton cards) should remain as-is since DashboardStats already has its own loading state via `isLoading` prop.
    Actually — remove the duplicate skeleton block in Home.jsx too if DashboardStats handles `isLoading` internally. Confirm by reading DashboardStats to see if `isLoading` skeleton is rendered (it is, in CardContent). So the outer skeleton in Home.jsx is redundant: remove the `isLoadingStats ? <grid of skeletons> :` conditional and just always render `<DashboardStats ... isLoading={isLoadingStats} ...>`.
  </action>
  <verify>
    <automated>grep -n "xl:grid-cols-7" /d/VSCode_Claude/01-Projetos/APP_DIROPS/APP_DIROPS_v2/src/pages/Home.jsx | wc -l</automated>
  </verify>
  <done>
    - Home.jsx has exactly 1 `<DashboardStats` usage
    - `xl:grid-cols-7` (the old inconsistent grid) is gone from Home.jsx
    - `TrendIndicator` component definition removed from Home.jsx
    - No duplicate Card blocks rendering KPIs in Home.jsx
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Consolidated Dashboard KPI layout — all 8 KPI cards now rendered exclusively by DashboardStats with text-xs labels, trend indicators, and consistent grid.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (or `pnpm dev`) from project root
    2. Navigate to the Dashboard / Home page (login first)
    3. At 1280px window width: confirm 8 KPI cards display in a single row with no text overflow or cut-off
    4. Confirm KPI labels (e.g. "Total Voos", "Chegadas Hoje") are legible — not tiny
    5. Confirm trend arrows appear on Total Voos and Pontualidade cards
    6. Confirm no duplicate KPI rows appear (should be exactly one row of 8 cards)
    7. Resize to 768px: confirm 4 cards per row (2-row layout)
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "text-\[10px\]" src/components/dashboard/DashboardStats.jsx` returns 0 lines
- `grep -c "xl:grid-cols-7" src/pages/Home.jsx` returns 0
- `grep -c "<DashboardStats" src/pages/Home.jsx` returns 1
- `grep -c "TrendIndicator" src/pages/Home.jsx` returns 0 (removed from Home)
</verification>

<success_criteria>
1. Dashboard KPI section renders exactly once with 8 cards in a single `xl:grid-cols-8` grid
2. All KPI label text uses `text-xs` or larger (no `text-[10px]`)
3. Trend indicators appear on Total Voos and Pontualidade KPIs
4. Home.jsx no longer contains inline KPI card markup or a duplicate TrendIndicator component
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-01-SUMMARY.md`
</output>
